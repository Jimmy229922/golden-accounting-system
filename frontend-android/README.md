# Frontend Android (Flutter - Planned)

This folder is intentionally kept as a placeholder for the future Flutter app.

## Status

- No Flutter code has been added yet, as requested.
- This app will consume the backend API from `../backend`.

## Suggested Next Step (when you are ready)

Run Flutter init inside this folder, then connect it to:

- `GET /api/health`
- future auth/accounting endpoints under `/api`
